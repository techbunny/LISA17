# Run a Docker Image

docker run -i -t -p 80:80 nginx:latest /bin/bash
docker run -d -p 80:80 nginx:latest

# Customize an Image

# Store Image in a Registry

az group create --name lisa17 --location eastus
az acr create --name lisa17reg2 --resource-group lisa17 --admin-enabled --sku Managed_Basic

az acr list --resource-group lisa17 --query "[].{acrLoginServer:loginServer}" --output table

docker tag nginx lisa17reg2.azurecr.io/ngnix:v1

docker push lisa17reg2.azurecr.io/ngnix:v1 

az acr repository list -n lisa17reg2 -o table
az acr repository show-tags -n lisa17reg2 --repository nginx -o table   <--- Doesn't work

